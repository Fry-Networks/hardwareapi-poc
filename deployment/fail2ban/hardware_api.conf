[Definition]
# Fail2ban filter to detect repeated 404 probes from the application logs.
# Matches lines like: FAILED_PROBE_404 ip=1.2.3.4 path=/foo ua="curl/7.68.0"

failregex = FAILED_PROBE_404\s+ip=(?P<host>\S+)\b

# Also allow matching the block event for logging/monitoring
ignoreregex = 
